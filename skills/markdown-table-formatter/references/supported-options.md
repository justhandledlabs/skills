# Supported Options

Modes: Markdown table formatting and CSV or TSV conversion.

Writes: stdout by default. In-place writes require --write and --force.

Scope: spacing and alignment only. Cell content is preserved.
